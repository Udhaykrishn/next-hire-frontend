# TypeScript Strictness Rules

- **STRICTLY FORBIDDEN**: Never use the `any` keyword. It is strictly avoided in this project.
- Use precise types, interfaces, or `unknown` if a type is truly dynamic.
- All function parameters and return types must be explicitly typed.
- Prefer `interface` for object definitions and `type` for unions or primitives.
